 int curMax = arr[0];
        int omax = arr[0]; //overall max

        for(int i = 1; i < arr.length; i++){
            if(curMax >= 0){
                curMax += arr[i];
            }
            else curMax = arr[i];

            if(curMax > omax){
                omax = curMax;
            }

        }
        System.out.println(omax);